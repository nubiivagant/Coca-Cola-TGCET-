import 'package:flutter/material.dart';

class  HomeProfileScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Profile')),
      body: Center(child: Text('Home Profile Page')),
    );
  }
}
